<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.engbaek.mapper.CourseAttachMapper">
	<insert id="insert">
		INSERT INTO courseAttach (coursePictureUuid, coursePictureName, uploadPath, courseCode)
		VALUES (#{coursePictureUuid}, #{coursePictureName}, #{uploadPath}, #{courseCode})
	</insert>
	
	<delete id="delete">
		DELETE FROM coursAttach
		WHERE coursePictureUuid = #{coursePictureUuid}
	</delete>
	
	<select id="findByCousreCode" resultType="com.engbaek.domain.CourseAttachVO">
		SELECT * FROM courseAttach
		WHERE courseCode = ${courseCode}
	</select>

	<delete id="deleteAll">
		DELETE courseAttach WHERE courseCode = #{courseCode}
	</delete>
</mapper>